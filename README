Para sincronizar esse repositorio Git tenha instalado os pacotes Git e Vim e
execute as seguintes linhas de codigo

cd ~
git clone http://github.com/hdamaich/dotvim.git ~/.vim
ls -s ~/.vim/vimrc ~/.vimrc

cd ~./vim
git submodule init
git submodule update
